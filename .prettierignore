# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
# Next.js auto-generates this file on every build with its own formatting
**/next-env.d.ts